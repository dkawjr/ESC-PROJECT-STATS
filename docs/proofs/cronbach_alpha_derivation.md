# Cronbach Alpha Derivation

For `k` items with covariance matrix `S`, alpha is:

`alpha = (k/(k-1)) * (1 - trace(S) / sum(S))`

Equivalent scalar form:

`alpha = (k/(k-1)) * (1 - sum(var_i) / var_total)`
