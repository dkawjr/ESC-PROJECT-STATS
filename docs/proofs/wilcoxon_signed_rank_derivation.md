# Wilcoxon Signed-Rank Derivation

After removing zero differences, rank absolute paired differences and assign signs. Under the null of symmetric differences, each sign assignment is equally likely over `2^n` possibilities. The exact distribution of the signed-rank statistic follows by enumerating all sign assignments.
